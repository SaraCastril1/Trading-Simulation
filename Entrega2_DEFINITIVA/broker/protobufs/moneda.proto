syntax = "proto3";

message message_parameters{
    string moneda = 1;
    string periodo = 2;
}

message ACK_parameters{
    bool ack = 1;
}


service Parameters{
	rpc send_parameters(message_parameters) returns (ACK_parameters);
}