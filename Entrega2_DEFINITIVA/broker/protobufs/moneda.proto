syntax = "proto3";

message message_parameters{
    string moneda = 1;
    string periodo = 2;
}
message ping{}

message ACK{
    bool ack = 1;
}


service Parameters{
    rpc conexion_up(ping) returns (ACK);
	rpc send_parameters(message_parameters) returns (ACK);
}

